{
  "name": "webcomponents-theme",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "provision": "docker run --name webcomponents-theme -u www-data -v $(pwd):/var/www/wordpress/wp-content/themes/$(basename $(pwd) )  --shm-size 2048m -p 80:80 -d torounit/wp-theme-test-env",
    "start": "docker webcomponents-theme"
  },
  "license": "GPL",
  "dependencies": {
    "lit-html": "^0.9.0"
  },
  "devDependencies": {
    "webpack": "^4.0.1",
    "webpack-dev-server": "^3.1.0",
    "workbox-webpack-plugin": "^2.1.3"
  }
}
